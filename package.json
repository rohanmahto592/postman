{
  "name": "postman",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "snowpack dev",
    "build": "rm -rf dist && mkdir dist && rsync -rv * dist --exclude ./dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.4.0",
    "bootstrap": "^5.2.3",
    "express": "^4.18.2",
    "jsoneditor": "^9.10.0",
    "pretty-bytes": "^6.1.0"
  },
  "devDependencies": {
    "snowpack": "^3.8.8"
  }
}
